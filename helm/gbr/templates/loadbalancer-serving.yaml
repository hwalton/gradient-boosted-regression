apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-serving
  labels:
    app: {{ .Release.Name }}-serving
  annotations:
{{ toYaml .Values.serving.service.annotations | indent 4 }}
spec:
  type: {{ .Values.serving.service.type }}
  ports:
    - name: http
      port: {{ .Values.serving.service.port }}
      targetPort: {{ .Values.serving.service.targetPort | default .Values.serving.service.port }}
  selector:
    app: {{ .Release.Name }}-serving